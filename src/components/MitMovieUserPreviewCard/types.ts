/** User fields needed for ChooseMIT combined invite card row */
export type MitMovieUserPreviewCounterpart = {
    id: string;
    username: string;
    profilePicture?: string;
    badge?: 'AKCRUIT' | 'GUARDIAN' | 'HERO' | 'SUPERHERO' | 'CELEBRITY';
};
