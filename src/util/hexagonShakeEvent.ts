/**
 * Event name only — kept in its own file so TabContext / screens never load hexagonShake.ts on Hermes.
 * Keep in sync with any native or legacy listeners.
 */
export const HEXAGON_SHAKE_EVENT = 'akcru.hexagonShake';
